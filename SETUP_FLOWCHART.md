# Setup Process Flowchart

```
┌─────────────────────────────────────────────────────────────────┐
│                    START: Access Beta Page                      │
│                 http://localhost:5173/beta                      │
└─────────────────────────────────────────────────────────────────┘
                                 ▲
                                 │
                                 │
┌────────────────────────────────┴────────────────────────────────┐
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 8: Start Development Server                       │    │
│  │ Command: npm run dev                                   │    │
│  │ Wait for: "Local: http://localhost:5173/"             │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 7: Update .env with Contract Addresses           │    │
│  │ Edit: .env file                                        │    │
│  │ Add: VITE_MOCK_USDC_ADDRESS=0x...                     │    │
│  │      VITE_MOCK_USDT_ADDRESS=0x...                     │    │
│  │      VITE_EST_TOKEN_ADDRESS=0x...                     │    │
│  │      VITE_YIELD_VAULT_ADDRESS=0x...                   │    │
│  │      VITE_EST_STAKE_ADDRESS=0x...                     │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 6: Deploy Smart Contracts                         │    │
│  │ Commands:                                              │    │
│  │   cd contracts                                         │    │
│  │   npx hardhat compile                                  │    │
│  │   npx hardhat run scripts/deploy.ts --network base...│    │
│  │ Wait for: Contract addresses in output                │    │
│  │ Check: contracts/deployments.json created             │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 5: Configure Private Key                          │    │
│  │ Edit: .env file                                        │    │
│  │ Add: PRIVATE_KEY=0xYourTestWalletPrivateKeyHere       │    │
│  │ ⚠️  Use TEST wallet only!                             │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 4: Create Environment File                        │    │
│  │ Command: cp .env.example .env                          │    │
│  │ Check: .env file exists                               │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 3: Get Base Sepolia Test ETH                     │    │
│  │ Visit: https://www.base.org/faucets                   │    │
│  │ Connect: MetaMask wallet                               │    │
│  │ Request: Base Sepolia ETH (free)                      │    │
│  │ Verify: Balance > 0.01 ETH                            │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 2: Install Dependencies                           │    │
│  │ Commands:                                              │    │
│  │   npm install                                          │    │
│  │   cd contracts && npm install && cd ..                │    │
│  │ Wait for: Installation complete                       │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 1: Clone Repository                               │    │
│  │ Commands:                                              │    │
│  │   git clone https://github.com/AgentForceHR/         │    │
│  │              latam-coin-hub.git                       │    │
│  │   cd latam-coin-hub                                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Prerequisites (Install First)                          │    │
│  │ • Node.js v18+ → https://nodejs.org/                  │    │
│  │ • Git → https://git-scm.com/                          │    │
│  │ • MetaMask → https://metamask.io/                     │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Alternative: Quick Setup Script Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    START: Access Beta Page                      │
│                 http://localhost:5173/beta                      │
└─────────────────────────────────────────────────────────────────┘
                                 ▲
                                 │
┌────────────────────────────────┴────────────────────────────────┐
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 5: Automated Process Runs                         │    │
│  │ • Installs dependencies                                │    │
│  │ • Compiles contracts                                   │    │
│  │ • Deploys to Base Sepolia                             │    │
│  │ • Updates .env automatically                          │    │
│  │ • Starts dev server                                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 4: Run Setup Script                               │    │
│  │ Commands:                                              │    │
│  │   chmod +x quick-setup.sh                             │    │
│  │   ./quick-setup.sh                                    │    │
│  │ Select: Option 5 (Run all steps)                     │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 3: Configure Private Key                          │    │
│  │ Commands:                                              │    │
│  │   cp .env.example .env                                │    │
│  │   nano .env                                           │    │
│  │ Add: PRIVATE_KEY=0x...                               │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 2: Get Base Sepolia Test ETH                     │    │
│  │ Visit: https://www.base.org/faucets                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                              ▲                                   │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Step 1: Clone Repository                               │    │
│  │ git clone https://github.com/AgentForceHR/            │    │
│  │           latam-coin-hub.git && cd latam-coin-hub     │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Time Estimates

| Step | Description | Time |
|------|-------------|------|
| 1 | Clone repository | 1 min |
| 2 | Install dependencies | 2 min |
| 3 | Get Base Sepolia ETH | 2 min |
| 4 | Configure private key | 1 min |
| 5 | Deploy contracts | 2 min |
| 6 | Update contract addresses | 1 min |
| 7 | Start dev server | 30 sec |
| **Total** | | **~10 min** |

---

## Decision Tree

```
Start
  │
  ├─ Do you have Node.js v18+?
  │   ├─ No → Install from nodejs.org → Continue
  │   └─ Yes → Continue
  │
  ├─ Have you cloned the repo?
  │   ├─ No → Run: git clone https://github.com/AgentForceHR/latam-coin-hub.git
  │   └─ Yes → Continue
  │
  ├─ Dependencies installed?
  │   ├─ No → Run: npm install && cd contracts && npm install
  │   └─ Yes → Continue
  │
  ├─ Have Base Sepolia ETH?
  │   ├─ No → Get from https://www.base.org/faucets
  │   └─ Yes → Continue
  │
  ├─ Is .env configured?
  │   ├─ No → cp .env.example .env → Add PRIVATE_KEY
  │   └─ Yes → Continue
  │
  ├─ Contracts deployed?
  │   ├─ No → cd contracts → npx hardhat run scripts/deploy.ts --network baseSepolia
  │   └─ Yes → Continue
  │
  ├─ Contract addresses in .env?
  │   ├─ No → Edit .env → Add VITE_*_ADDRESS variables
  │   └─ Yes → Continue
  │
  └─ Run: npm run dev
      │
      └─ Open: http://localhost:5173/beta
          │
          └─ SUCCESS! 🎉
```

---

## Troubleshooting Flow

```
Problem?
  │
  ├─ "Module not found"
  │   └─ Solution: npm install && cd contracts && npm install
  │
  ├─ "Insufficient funds"
  │   └─ Solution: Get Base Sepolia ETH from faucet
  │
  ├─ "Invalid private key"
  │   └─ Solution: Check PRIVATE_KEY in .env starts with 0x
  │
  ├─ "Cannot connect to wallet"
  │   └─ Solution: Install MetaMask → Switch to Base Sepolia
  │
  ├─ "Port 5173 already in use"
  │   └─ Solution: pkill -f vite → npm run dev
  │
  ├─ "Contract not deployed"
  │   └─ Solution: cd contracts → npx hardhat run scripts/deploy.ts --network baseSepolia
  │
  ├─ "Undefined contract address"
  │   └─ Solution: Check VITE_*_ADDRESS variables in .env
  │
  └─ Still stuck?
      └─ Read: SETUP_GUIDE.md for detailed instructions
```

---

## Visual Setup Process

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Clone   │───▶│ Install  │───▶│   Get    │───▶│   Add    │
│   Repo   │    │   Deps   │    │   ETH    │    │    PK    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
     │               │                │                │
     │               │                │                │
     ▼               ▼                ▼                ▼
  1 minute       2 minutes        2 minutes       1 minute

┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Deploy  │───▶│  Update  │───▶│   Start  │───▶│   Open   │
│ Contract │    │   .env   │    │   Dev    │    │  Browser │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
     │               │                │                │
     │               │                │                │
     ▼               ▼                ▼                ▼
  2 minutes       1 minute        30 seconds      SUCCESS!
```

---

## Files You Need to Edit

```
latam-coin-hub/
├── .env ◄─────────────────── Edit this file twice:
│                               1. Add PRIVATE_KEY
│                               2. Add contract addresses
│
└── That's it! Don't edit anything else.
```

---

## Commands Summary

```bash
# One-time setup
git clone https://github.com/AgentForceHR/latam-coin-hub.git
cd latam-coin-hub
npm install
cd contracts && npm install && cd ..
cp .env.example .env
# Edit .env to add PRIVATE_KEY
cd contracts
npx hardhat compile
npx hardhat run scripts/deploy.ts --network baseSepolia
cd ..
# Edit .env to add contract addresses

# Every time after setup
npm run dev
# Open http://localhost:5173/beta
```

---

## Quick Check Commands

```bash
# Verify everything is ready
node --version          # Should be v18+
ls .env                # Should exist
grep PRIVATE_KEY .env  # Should have 0x...
ls contracts/deployments.json  # Should exist
grep VITE_MOCK_USDC_ADDRESS .env  # Should have 0x...

# If all pass, you're ready!
npm run dev
```
